#include <stdlib.h>
#include "my_string.h"

int main(int argc, char* argv[])
{
  int i;
  MY_STRING hMy_string[100];//Create array of MY_STRING handles w/ 100 elements
  for (i = 0; i < 100; i++)//Init each element to NULL
    {
      hMy_string[i] = my_string_init_c_string(NULL);
    }
  //Use init_c to initialize the first element of the arra to "COPY ME!"
  //Write a for loop that uses assignment function to copy the first string into all other elements
  //Destroy every element with a for loop calling destroy
  //Print each string to screen using string insertion before deleting
  return 0;
}
